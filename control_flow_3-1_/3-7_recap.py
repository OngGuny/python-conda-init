# default import 되는 모듈이 있고 안되는 모듈이 python 에 있다.
"""
사용하기 위홰서는 import 해 줘야 한다.
from random (모듈) import randint(기능, function)
"""
# while
"""
true 일때, 계속 반복한다.
바로 false 를 넣으면 그냥 실행안하고 바로 넘어가버림

중요한건 while 내부의 코드에서는 exit 포인트를 정해줘야함. 
조건문을 flase 로 만드는 로직이 있어야함. 
"""
